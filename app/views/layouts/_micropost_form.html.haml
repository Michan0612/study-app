.container.micropost-container
  %h1 Form
  = form_with(model: @micropost, local: true) do |f|
    = render 'shared/error_messages', object: f.object

    .form-group
      = f.number_field :time, class: 'form-control', placeholder: "時間（分）を入力してください"
    .form-group
      = f.text_area :memo, class: 'form-control', placeholder: "メモを加えてください"
    .form-group
      = f.file_field :picture
    .form-group
      = f.submit "記録する", class: 'btn btn-info btn-lg form-submit'
